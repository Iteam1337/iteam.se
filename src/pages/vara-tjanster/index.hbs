---
subtitle: Våra tjänster
layout: default.hbs
navigation: true
order: 2
---

<div class="row services">

<div class="grid-full">
  <ul>
    {{#pages '{"route": "./src/pages/vara-tjanster/", "start": "", "type": "services"}'}}
      {{#each data}}
        <li class="{{this.frontmatter.categories}}">
          {{#if this.logo}}
            <img src="{{this.logo}}"/>
          {{/if}}
          <a href="{{this.url}}">{{this.title}}</a>
        </li>
      {{/each}}
    {{/pages}}
  </ul>
  </div>

</div>

{{> contact}}
