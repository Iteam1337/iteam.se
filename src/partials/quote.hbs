<div
  class="
    row
    partial
    quote
    {{#if background-color}} background-{{{background-color}}} {{/if}}
    {{#if color}} color-{{{color}}} {{/if}}
  "
  {{#if background-color}} style="background-color: {{{ hexFromString background-color }}}" {{/if}}>
  <div class="grid-full">
    {{#if image}}
      <img src="{{ image.src }}" alt="{{ image.alt }}">
    {{/if}}
    <div class="quote__quote">
{{#if quote}}
{{#markdown}}{{ quote }}{{/markdown}}
{{/if}}
{{#if mdtext}}
{{{md mdtext}}}
{{/if}}
    </div>
    <div class="quote__quotee">
      {{ by }}
    </div>
  </div>
</div>
