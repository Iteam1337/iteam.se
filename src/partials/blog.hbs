<div class="
  grid-half
  align-top
  blog
  {{#if color}} color-{{{color}}} {{/if}}
  {{#if background-color}} background-{{{background-color}}} {{/if}}
  " data-blog-hide-on-empty="true">
  <div class="row social-media">
    <div class="grid-full">
      <div class="blog-posts" data-blog-query="{{social-media.blog}}" data-blog-count="{{blog-count}}"></div>
    </div>
  </div>
</div>
