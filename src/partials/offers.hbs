<div class="row flex {{ type }} {{ background }}" {{#if background-color}} style="background: {{ background-color }}" {{/if}}>
  <div class="grid-full">
    <div class="row flex">
    {{#if offers}}
      {{#each offers}}
        <div class="grid-quarter">
          <i class="{{ class }}"></i>
          {{ title }}
        </div>
      {{/each}}
    {{else}}
      <ul class="all-offers">
        {{#pages '{"route": "./src/pages/vara-tjanster/", "start": "/vara-tjanster/", "type": "services"}'}}
          {{#each data}}
            <li>
              <i class="{{this.frontmatter.icon}}"></i>
              <div>
                <a href="{{this.url}}">{{this.title}}</a>  
                <p>{{this.frontmatter.description}}</p>
              </div>
            </li>
          {{/each}}
        {{/pages}}
      </ul>
    {{/if}}
    </div>
  </div>
</div>