<div class="row partial-{{ type }} {{ background }}" style="
  {{#if background-color}}
    background: {{{ hexFromString background-color }}}
  {{/if}}" >
  {{#each images }}
    <img src="{{ this }}" alt="">
  {{/each}}
</div>
