<div class="row">
  {{#pages route="./src/pages/varatjanster/" start="" type=""}}
    {{#each data}}
      {{#unless frontmatter.unpublished }} 
      <li>
        <a href="{{this.url}}">
          <i class="{{ this.frontmatter.icon }}"></i>
          {{this.title}}
        </a>
        <p>{{ this.frontmatter.description }}</p>
      </li>
      {{/unless}}
    {{/each}}
  {{/pages}}
</div>